# Copie para .env e preencha (ou configure como variaveis de ambiente no Render).
# Se deixar em branco, o sistema usa um arquivo SQLite local (devolucoes.db) automaticamente.

TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
